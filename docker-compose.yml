version: "3.9"

services:

  backend:
    build: ./Backend
    container_name: backend
    ports:
      - "8081:8080"

  frontend:
    build: ./Frontend
    container_name: frontend
    ports:
      - "80:80"